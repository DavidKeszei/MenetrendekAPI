enum RouteDirection {
  There,
  Back,
}

enum PartOfTheDay {
  Dawn,
  During_The_Day,
  Evening,
  None_Specified,
}

enum ExplanationType {
  Holidays,
  Normal,
}

enum StationType {
  Settlement,
  Station,
}

enum TicketType {
  Normal,
  Student,
  Ninty,
}
